[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
evm_version = "cancun"

remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/"
]

no_match_coverage = "(script|test)/.*"